# Backend Flask URL — used only for OAuth redirect (must bypass the Vite proxy)
# In production, set this to your actual backend URL or leave empty if same-domain
VITE_BACKEND_URL=http://localhost:5000
