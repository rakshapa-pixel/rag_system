# Flask backend base URL — used for all API calls and the Google OAuth button.
# Development: set to http://localhost:5000 so requests bypass the Vite proxy.
# Production (Vercel same-domain): leave unset — relative /api/* URLs work fine.
VITE_BACKEND_URL=http://localhost:5000
